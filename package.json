{
  "name": "studio-locater",
  "version": "1.0.0",
  "private": true,
  "description": "Local server for Yelp-backed studio search",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "node -e \"require('fs').accessSync('public/index.html'); console.log('OK: public/index.html')\"",
    "deploy": "wrangler deploy",
    "cms": "npm run dev --prefix studio"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  }
}
